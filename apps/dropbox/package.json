{
  "name": "dropbox-security",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3300",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest --no-threads",
    "big-bang": "pnpm database:down && pnpm database:up && pnpm migrate",
    "database:down:test": "docker-compose -p dropbox-test down --volumes",
    "database:down:dev": "docker-compose -p dropbox down --volumes",
    "database:down": "pnpm database:down:dev && pnpm database:down:test",
    "database:up:test": "docker-compose -p dropbox-test --env-file=.env.test.local up -d",
    "database:up:dev": "docker-compose -p dropbox --env-file=.env.local up -d",
    "database:up": "pnpm database:up:dev && pnpm database:up:test",
    "migration:generate": "drizzle-kit generate:pg --schema=./src/database/schema.ts",
    "migration:push": "npx ts-node ./src/database/migrate.ts .env.local && npx ts-node ./src/database/migrate.ts .env.test.local",
    "migrate": "pnpm migration:generate && pnpm migration:push",
    "inngest": "npx inngest-cli@latest dev -u http://localhost:3300/api/inngest"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.6.0",
    "@t3-oss/env-nextjs": "^0.7.1",
    "@vercel/postgres": "^0.5.1",
    "cookies-next": "^4.1.0",
    "date-fns": "^2.30.0",
    "drizzle-kit": "^0.20.4",
    "drizzle-orm": "^0.29.0",
    "dropbox": "^10.34.0",
    "elba-sdk": "workspace:*",
    "elba-msw": "workspace:*",
    "inngest": "^3.6.1",
    "js-cookie": "^3.0.5",
    "next": "14.0.1",
    "postgres": "^3.4.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "timekeeper": "^2.3.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.9.1",
    "@types/react": "^18.2.37",
    "@types/react-dom": "^18.2.15",
    "dotenv": "^16.3.1",
    "eslint": "^8.53.0",
    "eslint-config-custom": "workspace:*",
    "msw": "^2.0.8",
    "node-mocks-http": "^1.13.0",
    "tsconfig": "workspace:*",
    "typescript": "^5.2.2",
    "vitest": "^0.34.6"
  },
  "engines": {
    "node": "18",
    "pnpm": "8"
  },
  "packageManager": "pnpm@8.10.2"
}
